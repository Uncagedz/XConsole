# DriveCentric connector

The Manifest V3 extension and page-context parser remain in
`apps/drivecentric-extension`. This connector validates and normalizes extracted lead,
customer, conversation, and vehicle-interest payloads before gateway ingestion.

The first release never sends a customer message automatically.
