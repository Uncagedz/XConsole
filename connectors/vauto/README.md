# vAuto connector skeleton

TODO: capture sanitized active-inventory and vehicle-detail/price-history pages after
manual login/MFA. No selectors or API endpoints are assumed. `fixtures/vehicle.json`
is synthetic and supports unit validation only.
